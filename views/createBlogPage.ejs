<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Post - The Social Network</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #0d6efd; /* Light background */
    }
    .form-container {
      margin-top: 50px;
    }
    .form-container .card {
      background-color: #ffffff;
      border-radius: 25px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    h2{
      color: #0d6efd;
    }
    button{
      font-style: italic;
      height: 3rem;
    }
    hr{
        color: #ffffff;
        margin-top: 0px;
    }
  </style>
</head>

<body>

  <%- include('partials/_header') %>

  <%- include('partials/_navbar') %>

  <hr>

  <div class="container form-container mb-5">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="card p-4">
          <h2 class="text-center mb-5 fw-bold fst-italic">Create a Blog </h2>
          <form action="/post" method="post" enctype="multipart/form-data">
            <div class="mb-4">
              <input type="text" class="form-control" id="blogName" name="title"  placeholder="Add a title for your blog here" required>
            </div>
            <div class="mb-4">
              <textarea class="form-control" id="blogContent" name="content"  rows="6" placeholder="What's is on your mind ?" required></textarea>
            </div>
            <div class="mb-5">
              <p class="mb-2 text-muted">Upload thumbnail for your blog (image format only)</p>
              <input type="file"  class="form-control" id="imageUpload" name="blogThumbnail" required>
            </div>
            <div class="d-grid gap-2">
              <button type="submit" class="btn btn-primary ">Upload</button>
              <button type="reset" class="btn btn-secondary">Clear</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/_footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
